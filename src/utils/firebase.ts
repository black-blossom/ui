import { initializeApp } from 'firebase/app';

const firebaseConfig = {
  apiKey: 'AIzaSyD303kojNfutPUOi0hY4StpsLPrbiVw5QA',
  projectId: 'black-blossom',
};

const firebaseApp = initializeApp(firebaseConfig);

export default firebaseApp;
